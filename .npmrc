# Suppress deprecation warnings from transitive dependencies
# These are from dependencies of dependencies and will be resolved
# when those packages update their own dependencies
legacy-peer-deps=false
fund=false
audit=false

